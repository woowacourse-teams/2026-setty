# 배차 API 베이스 URL. 비워 두면 목(mock) 데이터로 동작한다.
# 실기기에서 로컬 서버에 붙일 때는 localhost가 아니라 PC의 LAN IP를 쓴다.
# 예) EXPO_PUBLIC_API_BASE_URL=http://192.168.0.10:8080
EXPO_PUBLIC_API_BASE_URL=

# 향후 UUID 헤더 인증 주입용(현재 미사용, 다음 이슈).
EXPO_PUBLIC_AUTH_UUID=
