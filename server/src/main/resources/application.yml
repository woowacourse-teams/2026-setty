aws:
  s3:
    region: ap-northeast-2
    bucket-name: techcourse-project-2026
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/setty
    username: setty
    password: setty
  jpa:
    hibernate:
      ddl-auto: update
      open-in-view: false
server:
  error:
    include-message: never
    include-stacktrace: never
setty:
  operator:
    secret: ${SETTY_OPERATOR_SECRET:}
  dispatch:
    seller-input-base-url: ${SETTY_SELLER_INPUT_BASE_URL:http://localhost:5173/seller-input}
    buyer-status-base-url: ${SETTY_BUYER_STATUS_BASE_URL:http://localhost:5173/dispatch}
