# SETTY 에이전트 가이드

## 공용 스킬

- 저장소 스킬의 원본은 `.agent-skills/`에 둔다.
- `.codex/skills`와 `.claude/skills`는 이 원본을 가리키는 심볼릭 링크다. 스킬을 변경하거나 추가할 때는 항상 `.agent-skills/`만 수정한다.

## 작업 흐름

1. 저장소 변경이 필요한 작업(코드·문서·설정)을 시작하기 전에 기존 이슈가 있는지 확인한다.
   - 단순 질문, 설명, 조사, 상태 확인처럼 저장소를 변경하지 않는 작업에는 이슈를 만들지 않는다.
2. 깃허브 이슈를 `setty-github-issue` 스킬을 통해 만든다.
   - 이슈를 만들기 전에 항상 사용자에게 확인을 받는다.
3. `이슈번호-브랜치명`으로 체크아웃한다.
   - `main`, `develop`에 직접 푸시하지 않는다.
4. 코드 작업을 진행한다.
5. `setty-github-pull-request` 스킬을 활용해 PR을 만든다.

## 개인 정보 관련

- 테스트에는 실제 이름, 전화번호, 상세주소, 사진이 아닌 가상 데이터를 사용한다.
- 비밀정보를 커밋하지 않고 환경 변수 또는 승인된 비밀 저장소를 사용한다.

## 커밋 규칙

모든 커밋은 정확히 `<type>: `으로 시작하고 하나만 선택한다.

- `build`: 빌드 시스템 또는 외부 의존성 변경
- `ci`: CI 설정 또는 스크립트 변경
- `docs`: 문서만 변경
- `feat`: 사용자 또는 운영자에게 새 기능 제공
- `fix`: 버그 수정
- `perf`: 측정 가능한 성능 개선
- `refactor`: 기능 추가나 버그 수정이 아닌 코드 구조 변경
- `style`: 코드 의미에 영향을 주지 않는 서식·표현 변경
- `test`: 테스트 추가 또는 기존 테스트 수정만 수행

### 예시

- docs: MVP 동안 Flyway 도입 보류 결정 반영
- chore: 개발용 MySQL docker compose 환경 추가
